# 작업 일지 (HANDOFF)

Codex와 Claude Code가 번갈아 이 프로젝트를 고칩니다. **작업을 시작하는 도구는 이 파일부터 읽고, 작업을 마치는 도구는 맨 위에 기록을 하나 더합니다.**
그러면 다음 도구가 무엇이 끝났고 무엇이 남았는지 바로 알 수 있습니다. 선생님도 이 파일로 진행 상황을 볼 수 있습니다.

## 쓰는 법 (에이전트용)
- **새 기록은 "기록" 제목 바로 아래(맨 위)에** 더합니다. 예전 기록은 고치지 않습니다(틀린 것이 있으면 새 기록에서 바로잡습니다).
- 틀은 아래 그대로, 짧고 사실대로 적습니다. 선생님이 읽을 수 있게 한국어로 씁니다.
- **확인한 것과 확인하지 못한 것을 나눠** 적습니다. 가짜 응답으로만 시험한 것은 "실제로는 확인 못 함"에 적습니다.
- "다음 할 일"은 다음 도구가 바로 집어 들 수 있게 구체적으로 적고, 끝낸 항목은 다음 기록에서 빼거나 "끝남"으로 옮깁니다.
- 기록을 더한 커밋을 올린 뒤 작업을 마칩니다.

```
### YYYY-MM-DD · 도구(Codex / Claude Code) · 브랜치
**한 일**
- …
**확인한 것** (npm run check / npm test 결과, 화면 확인 등)
- …
**확인하지 못한 것 · 알려진 문제**
- …
**다음 할 일**
- …
```

---

## 기록

### 2026-09-26 · Codex · codex/cli-images

**한 일**
- 앱에서 구독 Codex CLI를 실행해 그림을 만드는 로컬 서버와 맥 더블클릭 실행 파일을 추가했습니다. 이전 미병합 브랜치를 이어 작업했으며 main은 c265878입니다.
- 확정한 제작 목록에서 한 장 먼저/남은 그림 순차 생성, 진행 표시, 중단, 미리보기·개별 PNG 저장·프로젝트 자동 삽입을 구현했습니다. 제작 중 프로젝트·지시·기존 그림이 달라지면 자동 삽입을 중단하고 결과를 미리보기로 보존합니다.
- 서버는 127.0.0.1에만 열며 Host·Origin·토큰을 검사합니다. 고정 CLI 인수와 임시 작업 폴더를 사용하고 ChatGPT 로그인을 확인합니다. API 키 환경변수를 전달하지 않으며 유료 API로 전환하지 않습니다. 원본 PNG는 Git 제외 output/codex-images에 남습니다.
- README·AGENTS에 실행법, 기존 file:// 프로젝트의 백업/불러오기, 재접속과 이미지 검토 안내를 추가했습니다.

**확인한 것**
- npm run check: 28개 파일 통과. npm test: 기존 74/74 통과. npm run test:cli: 서버 접근 제한·입력 검증·순차 실행·실패·취소와 브라우저 자동 삽입·프로젝트 변경 보호 시험 통과(생성 응답은 모의).
- 설치된 Codex CLI 0.155.0-alpha.16.4의 실제 구독 세션으로 PNG 한 장을 만든 뒤, 실제 로컬 앱 버튼으로 명량대첩 삽화 한 장을 추가 생성해 미리보기·자동 삽입까지 확인했습니다. API 키를 사용하지 않았습니다.
- npm run screens 실행, 실제 생성 완료 화면(output/cli-live.png, docs/codex-cli.png)을 직접 확인했습니다.

**확인하지 못한 것 · 알려진 문제**
- 다른 컴퓨터/CLI 버전의 네이티브 이미지 도구 지원, 구독 한도 소진, 실제 장시간 생성 중단은 미확인입니다. 취소·오류는 모의 생성기로 시험했습니다. 자동화는 CLI 버전의 해당 옵션 지원에 의존합니다.
- CLI JSONL은 네이티브 도구 호출을 항상 노출하지 않습니다. PNG 파일 검증만으로 생성 출처를 증명하지는 않습니다.
- 새로고침 후 진행 작업에 자동 재연결하지 않습니다. 경고를 표시하며, 완료 원본은 output/codex-images에 보존합니다. file://와 HTTP 앱의 저장 공간이 달라 기존 자료는 백업으로 옮겨야 합니다.
- 실제 시험의 판옥선 요청에 거북선 형태가 섞이는 등 고증 오류가 있었습니다. 생성 그림의 역사적 정확성은 검증하지 않았습니다. 기존 API/음성 시험은 모의, 기존 의존성 경고는 남아 있습니다.

**다음 할 일**
1. 선생님의 기존 프로젝트를 로컬 실행 앱으로 옮겨 명량 자료 목록·화풍을 확인하고 나머지 그림을 제작하기.
2. 인물·선박·복식 일관성과 고증을 검토하고 필요한 그림만 다시 제작하기.
3. 생성 중 새로고침 시 작업 복구와 CLI 이미지 지원 사전 탐지를 개선하기. 브랜치는 main 미병합입니다.


### 2026-09-25 · Codex · codex/material-workflow

**한 일**
- 선생님이 프로젝트의 중심을 명확히 했습니다: 완성 영상보다 **완성 대본 → 제작 자료 목록 제안 → 검토·확정 → 삽화·지도 PNG·스토리 PPT·판서 자료**가 핵심입니다. 그림은 구독을 활용하며 기본 흐름에서 유료 API를 호출하지 않습니다.
- 최신 main은 `c265878`로 이전 기능 브랜치가 미병합임을 알리고, 그 기능을 포함한 새 브랜치에서 이어 작업했습니다.
- 첫 화면과 메뉴를 자료 제작 중심으로 구성했습니다. 원고를 보존하고 항목별 제작 여부·종류·그림 수·제작 지시·판서·인용문·출처·비교표·지도 지명을 검토·수정한 뒤 확정합니다.
- 선생님 제공 명량대첩 원고 전체를 예시에 넣고 13구간 맞춤 기획을 구성했습니다. 기본 제안은 삽화 11장, 위치 지도 1장, 인용문 3장, 비교표 1장, 판서 13장입니다. 일반 원고는 문단·인용·지명 규칙으로 초안을 만듭니다.
- 구독 AI에 기획을 다듬도록 요청하는 ZIP과 목록 JSON 재불러오기를 구현했습니다. 원고·구간 ID·항목 형식을 검증하고 원고 구간은 외부 JSON이 바꾸지 못하게 했습니다. 별도의 그림 주문서는 확정 뒤 받습니다.
- 각각의 PNG ZIP(1920×1080), 삽화 중심 스토리 PPT(표지+각 구간, 원고는 발표자 노트), 편집 가능한 판서 PPT를 받도록 했습니다. 그림이 없으면 초안 표시를 하고 완료된 삽화 PNG에 포함하지 않습니다. 재확정 시 같은 구간의 그림을 보존하고 지시가 바뀐 그림은 다시 그리기로 표시합니다.
- 지도는 등록 지명과 별칭을 사용하며 경로·해류·영토를 추정하지 않습니다. 긴 인용문 글씨가 출처 영역을 침범하지 않도록 조절했습니다. 사용법과 에이전트 안내를 갱신했습니다.

**확인한 것**
- `npm run check`: 27개 스크립트 통과. `npm test`: 기존 68개+새 자료 흐름 6개, 총 74개 모두 통과. 설치된 Chromium headless shell(build 1223)을 지정했습니다.
- 원고 전체 보존, 제안 단계에서 기존 장면을 바꾸지 않음, 목록 수정·선택·확정·저장·재열기, 잘못된 JSON 거절, 현재 대본과 다른 기획 거절, 그림 대기·PNG 크기·그림 보존·재생성 표시, 원고 변경 후 출력 차단을 시험했습니다.
- 실제로 생성한 지도 1장·인용/비교 4장·판서 13장의 PNG ZIP과 14장 스토리 PPT 구조·발표자 노트의 인용·마지막 문장을 확인했습니다. 새 PNG의 지도·긴 인용·비교표·판서 화면을 직접 확인했습니다.
- `npm run screens`로 화면을 갱신하고 새 첫 화면·검토 화면을 확인했습니다. 명량 자료 출력 화면을 휴대폰 폭 375px에서도 확인했고 가로 넘침은 없었습니다.
- 로컬 `output/material-workflow/`에 검토용 목록(`제작 목록.md`, `plan.json`)과 PNG 시험 결과를 보관했습니다. 용량이 큰 시험 파일은 Git에서 제외합니다.

**확인하지 못한 것 · 알려진 문제**
- 일반 대본 초안은 의미를 이해하는 AI 분석이 아니라 규칙 기반입니다. 필요하면 구독 AI 기획 요청서를 사용합니다. 이 요청서의 실제 외부 AI 응답 왕복은 이번에 수행하지 않았고 파일 검증·적용을 시험했습니다.
- 이번 원고의 삽화 11장을 실제로 새로 생성하지 않았습니다. 선생님이 목록을 검토·확정한 뒤 그림 주문서로 진행합니다. 기본 웹 앱이 구독 계정에 직접 로그인해 그림을 생성하는 기능은 아닙니다.
- 원고의 역사 사실·인용 번역 출처·병력 수·해류·전투 시간은 검증하지 않았습니다. 지도의 해안선은 기존 Natural Earth 자료이므로 울돌목 세부 전술도나 당시 영토도가 아닙니다.
- PPT는 구조와 노트를 확인했으며 실제 PowerPoint에서 열어 확인하지 않았습니다. 기존 API·마이크 시험은 모의 응답입니다. 이전의 의존성 보안 경고 2건은 남아 있습니다.

**다음 할 일**
1. 선생님이 자료 제작 → 명량대첩 원고 예시 → 제작 목록 제안받기에서 13구간을 검토하고 수량·화풍·판서를 조정한 뒤 확정하기.
2. 확정된 그림 주문서를 이 대화 또는 ChatGPT에 전달해 실제 삽화를 생성하고, 인물 외형 일관성·고증·파일명 연결을 확인하기.
3. 스토리/판서 PPT를 실제 편집 프로그램에서 확인하고 선생님의 자료 구성·분량 피드백 반영하기. 브랜치는 main 미병합 상태입니다.

### 2026-09-25 · Codex · codex/scene-editing

**한 일**
- 선생님이 고른 두 작업을 진행했습니다: 짧은 영상 제작 흐름 점검, 장면 나누기·합치기·끌어서 순서 바꾸기(A7).
- GitHub 최신 main(`c265878`)을 확인했습니다. 이전 점검 기록 브랜치가 미병합임을 알리고, 그 기록을 포함한 새 브랜치에서 이어 작업했습니다.
- ② 대본에 커서에서 나누기, 다음 장면과 합치기, 끌기 손잡이를 넣었습니다. ↑↓로도 옮길 수 있습니다. 편집·삭제 전 되돌리기 기록 저장에 실패하면 작업을 중단합니다.
- 그림 샷과 후보를 보존하고, 이어지는 샷 복제에는 새 ID를 줍니다. 순서 변경 시 썸네일·사실 확인·퀴즈의 장면 번호도 옮깁니다. 나누기·합치기는 길이를 재계산하고 사실 확인을 초기화합니다. 녹음 삭제는 확인 후 진행합니다.
- 명량 해전 소스 → 간이 대본·자료 → 이미지 주문서 → Codex 내장 도구로 실제 그림 2장 생성 → 파일명으로 불러오기 → 한국어 컴퓨터 음성 파일 → 약 15초 WebM 녹화·백업·전체 자료 ZIP 저장까지 진행했습니다. 상세 과정은 `docs/DEMO.md`입니다.
- README·AGENTS 사용법과 대본 화면 사진을 갱신했습니다. 용량이 큰 시연 결과는 로컬 `output/demo/`에 보관하고 Git에서는 제외합니다.

**확인한 것**
- `npm run check`: 24개 스크립트 문법 검사 통과. `npm test`: 기존 62개와 새 편집 시험 6개, 총 68개 모두 통과. 설치된 Chromium headless shell(build 1223)을 `CHROMIUM_PATH`로 지정했습니다.
- 새 시험: 커서 분할·문장 중간 샷 배치·저장·복원, 녹음 삭제 취소, 그림 후보 유지, 실제 마우스 끌기·키보드 이동, 연결된 자료 번호, 저장 실패·그림 생성 중 편집 차단, 삽화 보존·자료 장면 합치기.
- `npm run screens` 실행 후 최종 대본 화면을 직접 확인했습니다. 시연 프로젝트의 휴대폰 폭(375px) 화면도 확인했습니다. 커밋에는 변경된 대본 화면 사진만 포함합니다.
- 시연 영상은 1280×720, 앱 타임라인 14.74초, 5,263,930바이트입니다. 내려받은 영상의 5초 화면과 음성 해독·무음 아님을 확인했고 자바스크립트 오류는 없었습니다.

**확인하지 못한 것 · 알려진 문제**
- 실제 Claude 대본·사실 확인 API와 OpenAI·Gemini 이미지 API는 사용하지 않았습니다. 자동 시험의 API·마이크 응답은 가짜입니다. 실제 이미지 생성은 Codex 내장 도구로 별도 검증했습니다.
- 선생님 마이크 녹음·음성 청취 품질, PPT를 PowerPoint로 열기, 편집 프로그램 MP4 호환성, 생성 그림의 역사 고증은 미확인입니다. 시연 음성은 macOS Yuna이며 앱에 TTS 기능을 새로 넣은 것은 아닙니다.
- 합치기는 앞 장면의 종류·자료·인물·전환 설정을 유지합니다. 삽화 그림 목록은 연결하고 SVG·자동 배경은 정지 그림으로 바꿉니다. 녹음이 있는 장면을 나누거나 합치면 확인 후 녹음을 지우므로 다시 녹음해야 합니다. 변경 전 원본은 되돌리기 기록에 남습니다.
- 나누기·합치기 후 기존 수업 자료는 내용 재검토가 필요합니다. 앞서 발견한 의존성 보안 경고 2건은 이번 범위에서 수정하지 않았습니다.

**다음 할 일**
1. 선생님이 로컬 시연 영상과 `output/demo/project-backup.json`을 확인하고, 실제 녹음과 원하는 역사 자료로 제작해 보기. 이번 브랜치는 아직 main에 합치지 않았습니다.
2. 생성 그림의 복식·배·깃발·지형을 검수하고, 실제 제작에서 나온 문제를 우선 수정하기.
3. API 키를 브라우저 설정에 직접 넣은 환경에서 실제 AI 대본·이미지 기획을 확인하기. 채팅이나 Git에 키를 넣지 않기.
4. 의존성 경고 영향·해결 방법 검토. 수업 발표 모드(A4)와 기타 제안은 미착수입니다. A7의 기본 편집 기능은 완료했습니다.

### 2026-09-25 · Codex · codex/project-review

**한 일**
- GitHub main의 최신 커밋(`c265878`)을 가져와 안내 문서와 작업 일지를 읽고 현재 상태를 점검했습니다. 기능 코드와 기존 기록은 고치지 않았습니다.
- 테스트 의존성을 `npm ci`로 설치했습니다. 다음 작업은 선생님이 고른 뒤 시작합니다.

**확인한 것**
- `npm run check`: 23개 파일 문법 검사 통과.
- `npm test`: 62개 모두 통과. 기본 시험용 브라우저 설치가 압축 해제 도중 진행되지 않아, 이미 설치된 Chromium headless shell(build 1223)을 `CHROMIUM_PATH`로 지정했습니다.
- 재실행 명령(macOS 현재 환경): `CHROMIUM_PATH="$HOME/Library/Caches/ms-playwright/chromium_headless_shell-1223/chrome-headless-shell-mac-arm64/chrome-headless-shell" npm test`.
- GitHub main이 위 커밋인 것을 확인했습니다. 이번 점검 기록은 별도 브랜치에서 관리하며 main 병합은 선생님이 결정합니다.

**확인하지 못한 것 · 알려진 문제**
- Claude·OpenAI·Gemini·YouTube 응답과 마이크는 가짜 자료로만 시험했습니다. 실제 API 연결, 실제 녹음, 실제 그림 생성부터 영상 완성까지는 확인하지 않았습니다.
- 프로젝트 지정 Playwright 브라우저(build 1194) 설치는 완료하지 못해 중단했습니다. 테스트는 기존 브라우저에서 통과했으며, 지정 버전에서의 결과는 미확인입니다.
- `npm audit`에서 `image-size`와 이를 사용하는 `pptxgenjs`에 높은 심각도 경고 2건이 나왔습니다. 앱에 미치는 실제 영향은 미확인이고 의존성 자동 수정은 하지 않았습니다.
- 실제 PPT 열기, 외부 글꼴, 편집 프로그램과의 MP4 호환성 등 이전 일지의 미확인 사항은 그대로 남아 있습니다.

**다음 할 일**
1. 가장 먼저 추천: 짧은 역사 영상 한 편으로 소스 → 대본 → 이미지 주문서 → 실제 그림 생성·불러오기 → 영상 저장을 확인하기. 실제 목소리 녹음과 최종 내용 확인은 선생님과 함께 진행합니다.
2. 실사용에서 드러난 문제를 먼저 고치기. 의존성 보안 경고의 영향과 해결 방법도 검토하기.
3. 새 기능을 고른다면 장면 나누기·합치기·순서 바꾸기(A7), 또는 수업 발표 모드(A4)를 추천합니다. 아직 구현은 시작하지 않았습니다.
4. 그 밖의 기존 제안(A2 쇼츠 자동 추출, A3 교육과정 시리즈, A5 영어 자막, A6 댓글 기획, B1 AI 목소리)은 미착수입니다.

### 2026-09-25 · Claude Code (웹) · claude/ecstatic-ramanujan-1xds35 → main

**한 일 (처음부터 지금까지 요약)**
- 저장소를 만들고(`claudecodeadivicer`에서 옮겨 옴), 사관 스튜디오를 `index.html` 하나로 열리는 앱으로 만들었습니다. 커밋 10개, 모두 `main`에 합쳤습니다.
- 소스 → 대본: 글·PDF·사진·유튜브 스크립트(사실 자료 / 구성·말투 참고) → Claude 구조화 출력(키가 없으면 간이 방식). 장면 고치기, 사실 확인.
- 🎨 이미지(중심 기능): 장면마다 샷 1~3장(전경·사건·인물·유물·그림 지도), 화풍 6종, 등장인물 설정집, Claude 기획,
  Codex·ChatGPT 구독용 **이미지 주문서** ZIP과 파일 이름(`S03-2_<id>.png`)으로 불러오기, 이미지 API(OpenAI·Gemini) 생성,
  🔁 다시 그리기(고칠 점 → Claude가 프롬프트 수정, 후보 보관·되돌리기), 대기열(동시 1~3장, 재시도, 실패만 다시).
- 영상: 켄 번스·SVG 시차·샷 전환·역사 장면 4종(사료·연표·인물 관계도·비교표)·그림 지도·이름표·장면 전환·핵심어 자막·
  내 캐릭터(손그림 스티커)·목소리 녹음(프롬프터)·배경음악(말할 때 줄임)·쇼츠 9:16·MP4/WebM 녹화.
- 결과물: 스토리·판서·퀴즈 PPT, 지도 PNG/영상, 판서 쓰는 영상, 손그림 → 분필 그림·그리는 영상, 자막 SRT·챕터·업로드 정보·썸네일,
  학습지(학생용·교사용), 모두 받기 ZIP, 한 번에 만들기(파이프라인).
- 여러 프로젝트, 되돌리기 기록, IndexedDB 저장, `AGENTS.md`·`CLAUDE.md`·`docs/CODEX.md`, 이 작업 일지.

**확인한 것**
- `npm run check` 통과, `npm test` 62개 모두 통과(Claude·OpenAI·Gemini·YouTube oEmbed 응답과 마이크는 가짜).
- 주요 화면은 스크린샷으로 확인(`docs/*.png`), 가로·세로(쇼츠)·휴대폰 폭 넘침 없음.

**확인하지 못한 것 · 알려진 문제**
- 실제 API로 한 번도 돌려 보지 않음: Claude(대본·이미지 기획·사실 확인 등), OpenAI·Gemini 이미지(모델 이름이 바뀌었을 수 있음 → 설정의 모델 칸).
- **Codex로 그림 만들기**를 실제로 해 보지 않음. Codex에서 이미지 생성을 쓸 수 있는지는 요금제·버전에 따라 다름(안 되면 ChatGPT 앱 방식).
- 실제 유튜브에서 복사한 스크립트, 실제 마이크 녹음, 종이 사진 손그림의 바탕 지우기는 흉내 낸 자료로만 시험.
- 판서 분필 글꼴(`Nanum Pen Script`)·썸네일 글꼴(`Black Han Sans`)은 인터넷에서 불러옴 — 이 작업 환경에서는 대체 글꼴로만 봄.
- MP4는 브라우저가 H.264를 지원할 때만 편집 프로그램과 잘 맞음(시험 브라우저는 VP9-in-MP4). PPT는 파일 구조만 확인(LibreOffice로 열어 보지 못함).
- 대기열은 창을 닫으면 비워짐(만든 그림은 저장됨).

**다음 할 일**
1. **실사용 한 편 끝까지 해 보기**(선생님): 소스 → 한 번에 만들기 → 🎨 이미지 주문서 → Codex로 그림 → 불러오기 → 목소리 → 영상 녹화. 막힌 곳을 이 일지에 적기.
2. 실사용에서 나온 문제 고치기(가장 먼저).
3. 제안만 하고 아직 안 만든 것: 긴 영상 → 쇼츠 자동 추출(A2), 교육과정 성취기준 기반 시리즈 기획(A3), 수업 발표 모드(A4),
   영어 자막 번역(A5), 댓글로 다음 영상 기획(A6), 장면 나누기·합치기·끌어서 순서 바꾸기(A7), AI 목소리 TTS(B1, 다른 회사 키 필요).
